jQuery(document).ready(function( $ ){

/**
* Hide notices from jQuery cause display:none method using CSS sometimes glitching on the wp-admin
**/
$(".notice.notice-error").hide();
$(".notice-warning").hide();



});